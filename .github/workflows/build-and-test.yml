name: Build and Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout with submodules
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Setup .NET 10
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'

    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2

    - name: Setup NuGet
      uses: nuget/setup-nuget@v2

    - name: Restore NuGet packages
      run: nuget restore BAnalyzer.sln

    - name: Build solution (Release|x64)
      # Override PlatformToolset to v143 (VS 2022) since the GitHub-hosted runner
      # does not include the v145 (VS 2025) toolset used by the local dev environment.
      run: >
        msbuild BAnalyzer.sln
        /p:Configuration=Release
        /p:Platform=x64
        /p:PlatformToolset=v143
        /m
        /verbosity:minimal

    - name: Run tests
      run: dotnet test x64/Release/BAnalyzerCoreTest.dll --logger "trx;LogFileName=test-results.trx" --results-directory TestResults

    - name: Upload test results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-results
        path: TestResults/**/*.trx

    - name: Publish build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: BAnalyzer-Release-x64
        path: |
          x64/Release/*.exe
          x64/Release/*.dll
          x64/Release/*.json
          x64/Release/runtimes/**/*
        retention-days: 30
